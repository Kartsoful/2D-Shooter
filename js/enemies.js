// Updated logic for checking health based on enemy type.
if (e.type === 'tank') {
    // Checks for tanks' health
    if (e.hp <= 0) {
        // Handle tank destruction
    }
} else {
    // Checks for other enemy types' health
    if (e.health <= 0) {
        // Handle destruction of other types
    }
}